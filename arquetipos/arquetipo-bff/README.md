# Arquetipo Maven — BFF

Base para generar un nuevo Backend For Frontend.

## Uso
```bash
mvn archetype:generate \
  -DarchetypeGroupId=cl.duoc.sanosysalvos \
  -DarchetypeArtifactId=arquetipo-bff \
  -DarchetypeVersion=1.0.0
```
